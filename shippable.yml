language: php

php:
  - 5.6

before_script:
   - mkdir -p shippable/testresults
   - mkdir -p shippable/codecoverage

script:
   - phpunit  --log-junit shippable/testresults/junphpunit.xmlit.xml --coverage-xml shippable/codecoverage tests